image: frekele/gradle

tests:
    tags:
        - java
    only:
        - master
    script:
        - gradle test

    artifacts:
        when: always
        name: reports
        paths:
            - reports/

# after_script:
#     - ls -R -a
